#!/bin/bash
if [ -n DEVICE_CLANG ]; then
  make CC=$KERNEL_CLANG CROSS_COMPILE=$KERNEL_CCOMPILE64 O=out ARCH=$DEVICE_ARCH -j4 $1 $2 $3 $4 $5
else
  make CC=${KERNEL_CCOMPILE64}gcc CROSS_COMPILE=$KERNEL_CCOMPILE64 O=out ARCH=$DEVICE_ARCH -j4 $1 $2 $3 $4 $5
fi
